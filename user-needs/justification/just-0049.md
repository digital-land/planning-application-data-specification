---
id: just-0049
status: proposed
needs:
  - dd-need-094
  - dd-need-095
  - data-need-005
satisfied_by:
  allOf:
    - dataset: site
      field: address-text
    - dataset: site
      field: name
satisfaction: partial
confidence: high
notes:
---

The `address-text` field lets a site be identified and found using a familiar location description where a usable address is available.

Together, `address-text` and `name` support users who recognise places through addresses, building names, street descriptions or other plain-language labels rather than through map geometry or system references alone.
