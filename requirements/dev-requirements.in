-r requirements.in
flake8
black
isort
pytest
# Keep in sync with base lock (pulled in via black)
pathspec==1.0.3
# Keep in sync with base lock (pulled in via black)
platformdirs==4.5.1
# Keep in sync with base lock (pulled in via black)
tomli==2.4.0
